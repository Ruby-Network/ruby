version: "3"
services:
  ruby:
    image: ruby:latest
    build: .
    container_name: ruby
    restart: unless-stopped
    ports:
      # DO NOT CHANGE 9293!
      - 9001:9293
    volumes:
      - ./config:/usr/src/app/config
#networks:
#  default:
#    external:
#      name: default_net
